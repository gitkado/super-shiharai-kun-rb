{
  "model": "claude-opus-4-5",
  "permissions": {
    "defaultMode": "plan",
    "allow": [
      "Read",
      "Edit",
      "Write",
      "MultiEdit",
      "Glob",
      "Grep",
      "LSP",
      "Task",
      "TodoWrite",
      "WebFetch",
      "WebSearch",
      "NotebookEdit",
      "Bash(bundle:*)",
      "Bash(bin/rails:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git branch:*)",
      "Bash(git show:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Bash(ls:*)",
      "Bash(cat:*)",
      "Bash(mkdir:*)",
      "Bash(touch:*)",
      "Bash(cp:*)",
      "Bash(mv:*)"
    ],
    "deny": [
      "Bash(rm:*)",
      "Bash(git push:*)",
      "Bash(git reset --hard:*)",
      "Bash(git clean:*)",
      "Bash(sudo:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bundle exec rspec --fail-fast || echo 'テスト失敗。Claudeに修正を依頼してください。'"
          },
          {
            "type": "command",
            "command": "bundle exec rubocop && bundle exec packwerk check && bundle exec brakeman -q"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|run_bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc '\n# 受け取ったツール呼び出しのペイロード(JSON)を標準入力から読む\n# run_bash のときは .command または .args にコマンド文字列が入る想定\nPAYLOAD=$(cat);\nCMD=$(printf %s \"$PAYLOAD\" | jq -r \".command // .args // \\\"\\\" \");\n# git push を検出したらエラー終了（Claudeの実行を止める）\nif printf %s \"$CMD\" | grep -Eq \"(^|[[:space:]])git[[:space:]]+push(\\b|--)\"; then\n  echo \"🚫 git push はClaudeから実行できません。手動で実行してください。\" 1>&2\n  exit 1\nfi\nexit 0\n'"
          }
        ]
      }
    ]
  },
  "env": {
    "MAX_THINKKING_TOKENS": "31999"
  },
  "disabledMcpjsonServers": ["serena"]
}
